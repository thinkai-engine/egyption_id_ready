# Egyptian ID OCR - Two-Stage Detection Requirements
# ===================================================
# Minimal requirements for running two-stage YOLO detection
# 
# Usage: pip install -r requirements-detection.txt

# Core dependencies
numpy==1.26.4
opencv-python-headless>=4.10.0
Pillow>=10.0.0
tqdm==4.66.4
pandas==2.2.2

# YOLOv8 for detection
ultralytics>=8.0.0

# EasyOCR (optional, for fallback)
easyocr>=1.7.0

# Jupyter (optional, for notebooks)
jupyter>=1.0.0
matplotlib>=3.5.0
ipywidgets>=7.0.0
